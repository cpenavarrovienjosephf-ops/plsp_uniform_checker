# Empty — lets AuthContext fall back to '/api' which goes through the Vite proxy
# DO NOT set VITE_API_URL to http://localhost:5000/api — that bypasses the proxy
# and causes the browser to strip the Authorization header on cross-origin requests
VITE_API_URL=/api
